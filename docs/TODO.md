# 代办事项

[ ] **节点名称过长**：首页连接的节点信息中名称显示重叠，需截断显示...。✅ 已修复2. **节点测速体验**：延迟数字在测速过程中使用加载中动画（骨架屏 / 转圈），有结果后再展示数值。3. **网速真实性**：核实仪表盘网速是否为真实采样；若仍为占位，需对接 Mihomo 流量或连接统计 API。4. **网络信息与出口 IP**：通过 **ip.sb**（或项目内约定 URL）请求并展示当前出口 IP / 地理信息。5. **存储轻量化**：去除 SQLite，改用轻量配置（如本地 JSON / Tauri 内建存储）持久化服务商与节点数据。
